#  type constrain functions

def is_actor(text):
    """
    word_list = text.split(" ")
    
    # if either word is lwoercase, its not an actor
    for word in word_list:
        if not word[0].isupper:
            return False

    if text is in IMDB database:
        if IMDB(actor).deathYear before 2013:
            return False

        return True
    """

def is_award(text):
    """
    word_list = text.split(" ")

    for word in word_list:
        if not word[0].isupper:
            return False

    if word_list[0] == "Best": 
        return True
    if word_list[1] is an attribute or role:
        return True
    """

    
        
    